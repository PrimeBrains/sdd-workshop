<!--
  フロー・スコープ提案テンプレート — kiro-scenario-flow が §1 スコープ承認ゲートで使う。
  ★これは「発案」ではなく「どのユニットを・どの順で・どこからどこまで一つのフローにするか／何をスコープ外にするか」の
    提案。AI が候補を埋め、人間が明示承認（ratify）する。通しのフロー固有の価値・観点は、承認後に AI が draft で生成し、
    そのドラフトを人間が改めて批准する（§4）。この段では AI は通しの価値を書かない。
  {{...}} を実値で置換し、AskUserQuestion で人間の明示承認を取る（その記録がゲートの SCOPE_APPROVED の根拠）。
-->
# フロー・スコープ提案: {{TITLE}}（slug: {{SLUG}}）

## このフローの範囲（どこから・どこまで）
- 起点（先頭メンバーの precondition）: {{FLOW_PRECONDITION}}
- 終点（末尾メンバーの postcondition）: {{FLOW_POSTCONDITION}}

## 合成するユニット（順序・全て agreed であること）
| # | ユニット | status | このフローでの一行の役割 |
|---|---|---|---|
| 1 | units/{{MEMBER_1}} | {{STATUS_1}} | {{ROLE_1}} |
| 2 | units/{{MEMBER_2}} | {{STATUS_2}} | {{ROLE_2}} |

## スコープ外（この通しに含めない・別フロー後送り）
- {{OUT_OF_SCOPE_1}}（理由: {{OOS_REASON_1}}）

## 人間の承認を求める点（AskUserQuestion で確定）
- 上記の **ユニット集合・順序・起点/終点・スコープ外の線引き** で合意できるか。
- **未 agreed メンバーがある場合**: 先に `kiro-scenario` で agreed へ昇格するか、フローを draft 据え置きで成熟を待つか。

<!--
  承認が取れたら §2（接地）→ §3（AI がドラフトで通しの価値を生成）→ §4（人間がドラフトを批准）→ §5（検証ループ）へ。
  この提案ファイルは恒久成果物ではない（成果物は flows/{slug}.md）。承認記録は会話ログ／Flow Verdict に残す。
-->
