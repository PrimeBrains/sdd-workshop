# Moira プロパティ目録（不変条件カタログ）v0.1 draft

状態: **draft（人間批准前）**。方針は [.kiro/steering/moira-verification.md](../.kiro/steering/moira-verification.md)、正典は [MODEL.md](MODEL.md)、指標定義は [.kiro/steering/moira-naming.md](../.kiro/steering/moira-naming.md)。

本目録は、MODEL の不変条件（オラクル）を**人間がレビューできる平易な一文**に落とし、実行可能なテスト（PBT・境界モデル検査・否定）の**一次仕様**とするもの。実行テストはこの一文の*射影*にすぎない。

## レビューの仕方（これだけ）

- **「平易な一文」列だけ**を読み、「うん、それが私の意図」か「いや違う」を判定する。コードは見なくてよい。
- AI（**実装とは別エージェント**）が一文を `proposed` で起こし、人間が `agreed` で批准／差し戻す。＝MODEL 自身の `proposed→agreed` 規律をプロパティに自己適用。
- 専門用語・skill 実名は本文に出さない（シナリオ README と同等の言語規約）。

## 規約

- **根拠は安定 clause ID（A/I/P/R/§）で参照**（行番号は使わない）。定義は MODEL／moira-naming に従い、矛盾時は MODEL が勝つ。
- 各行は「**固定する量**」と「**意図的に FREE な量**」を必ず持つ（何を縛り何を開けるかを人間が見える化）。
- **現状**は静的所見であり、テストは未実装（実行結果ではない）。`要修正` は実装乖離の疑い。
- 計器: **PBT**=参照実装に対する性質テスト／**MC**=MODEL を仕様とする境界モデル検査／**DENY**=書かない・否定プロパティ。

---

## A. PBT（参照実装 `moira/backend` に対する性質）

| ID | 根拠 | 人間がレビューする一文 | 固定する量 | 意図的に FREE | 現状 |
|---|---|---|---|---|---|
| PR-EVPCT-RANGE | R-U8・P1 | **見積に人が合意しないまま完了した作業は、出来高(EV)として計上しない**（EV は合意済み領域だけを語り、未合意分はカバレッジ低下として可視化）。その結果、達成率 EV% は常に 0〜100% に収まる | EV%∈[0,1]／未合意完了は EV_abs 非算入 | 具体的な EV% 値・木の形 | 未実装（静的: ev.ts は合意済みのみ算入＝満たすと見られる） |
| **PR-DONE-LOCK ★** | I4・R-E3 | いったん**完了して出来高が確定した**作業の出来高(EV_abs)は、後から何をしても**減らない**。完了済みを変えたいときは*やり直し*ではなく**新しい行を立てて差し替え**（旧行の出来高は累積に残り、"今の進捗"だけ新行に移る）。見積のやり直しで出来高や率が動くのは、**まだ完了していない**作業だけ | 完了ノードの EV_abs 不変 | 未完了ノードの EV%・カバレッジは動いてよい | **未実装（静的: 現実装は違反の疑い＝完了ガード無し。要修正）** |
| PR-I1-ROLLUP | I1 | 親の見積は、**合意済みの子**の見積の合計に一致する。まだ見積もっていない子は『カバレッジ低下』として現れ、整合の破れにはしない | 親=Σ(合意済み子) | 未見積子の存在（カバレッジで表現）・木の深さ | 未実装（想定: 緑） |
| PR-AC-ROLLUP | P3 | 実コスト(AC)はツリーを下から積み上げた合計。各ノードのACは「**そのノードに直接付いたコスト＋子のACの合計**」で、コストは普通*実際に作業する葉*に付くため、集約だけの親ノードのACは実質「子のACの合計」になる | AC=自コスト+ΣAC(子)（任意の木で成立） | 具体額・どのノードにコストが付くか | 未実装（想定: 緑） |
| PR-CANCEL-EXCL | R-C2 | タスクをキャンセルしても、有効分の出来高(EV_abs)は**増えない**。キャンセルは稼働対象から外れる | EV_abs 非増・active basis 除外 | サンク EV_abs の値 | 未実装（想定: 緑） |
| PR-CANCEL-SUNK | R-C2・A6・P6 | キャンセルしても**費やした実コスト(AC)はサンクとして残る**（消えない・偽の完了にしない）。**出来高(EV)は偽装されず、稼得済み（完了済みサブ単位）分のみサンク EV_abs に出る**——進行中で未完了ならサンク EV_abs=0、コストだけがサンクとして残る | ①AC は cancelled のコストを保持（A6/P6）②サンク EV_abs＝完了済みサブ単位の稼得のみ（未完了なら0；R-C2） | サンク額 | 未実装（①ac.ts は静的確認済 ②要 完了サブ単位→cancel 到達 witness） |
| PR-CANCEL-INVISIBLE | R-C2・P4 | キャンセル済みノードは作業キュー・レビューキュー・未割当バックログの**いずれにも現れない**（『これ何だっけ?』を生まない） | 全キュー・バックログから除外 | 表示順・分類 | 未実装（想定: 緑） |
| PR-CUM-GE-EFF | R-S5 | 過去に実際に働いた**累積出来高**は、**現行有効分**の出来高を下回らない（置換された旧タスクの働きは累積に残る） | 累積EV ≥ 現行有効EV | 差の値 | 未実装（要 supersede 到達 witness） |
| PR-COST-NEUTRAL | P6・R-U10 | コストを足しても実コスト(AC)が増えるだけで、達成率や予定価値(EV系)は**1ミリも動かない**（時間とコストは別軸） | cost 追記で EV%/EV_abs/PV/SPI 不変 | AC 増分 | 未実装（想定: 緑） |
| PR-COST-DEDUP | §2.8 | 同じコスト記録(同じ id)を二重に入れても**二重計上されない**。一方、本当に二度働いた(別 id)場合はちゃんと加算される | 同 id は no-op／別 id は加算 | 額 | 未実装（静的: seenCostIds 確認済） |
| PR-PV-EXCL | §3 | 予定価値(PV)には、**合意済みかつスケジュール済み**のタスクだけが入る。合意だけ／スケジュールだけのタスクは入らず、可視ギャップになる | PV の3除外 | 具体 PV 値 | 未実装（静的: pv.ts 確認済） |

### メタモルフィック（PBT）

| ID | 根拠 | 人間がレビューする一文 | 固定する量 | 意図的に FREE | 現状 |
|---|---|---|---|---|---|
| PM-ORDER-INV | I3 | イベントの**記録順を入れ替えても**（同じ時刻・同じ id の並びを保つ限り）、導出結果は完全に同じになる | (ts,id) 保存の並べ替えで導出同一 | 入力配列の物理順 | 未実装（前提: id 一意性→ MC-IDUNIQ） |
| PM-SCALE-INV | P1 | 全タスクの見積を k 倍すると、出来高・予定価値・実コストは k 倍になるが、**達成率・SPI・CPI・各カバレッジは変わらない**（比率だから）。完了予定日などの**時間軸は k 倍にならない＝対象外** | 指標・予算次元のスケール則／時間軸は非対象 | k 値 | 未実装 |
| PM-CAP-MONO | §3・R-S7 | 容量 c を下げると、どのタスクの完了**予測**も早くはならず、**予算総額は変わらず**、凍結済みのベースライン・スロットは動かない | c 減で予測非早期化・予算総額不変・凍結スロット不動 | 具体日付 | 未実装（rederive.test が一例） |

---

## B. MC（MODEL を仕様とする境界モデル検査）

スコープを明示し、その範囲を全数列挙する。連続量（日付・c・予算）は対象に入れない。

| ID | 根拠 | 人間がレビューする一文 | 固定する量 | 検証スコープ | 現状 |
|---|---|---|---|---|---|
| MC-LIFECYCLE | §2.5 | ライフサイクルは決められた順路しか辿れない。**不正な遷移**（例: 受入済みから実装中へ）が、完了としての出来高を生むことは**絶対にない** | 不正遷移が完了寄与を生まない | ノード1・遷移列≤6 | 未実装（静的: 実装にガード無し＝乖離。開示・要検査） |
| MC-CONFLICT | I3・R-U12 | 2人が同じタスクに違う値で**同時刻に**合意しても、現行値は決定的に1つに定まり、**必ず矛盾警告が立つ** | 全順序で現行値決定的＋警告 | ノード1・actor2・イベント2 | 未実装 |
| MC-IDUNIQ | I3 | 2つの別々のイベントが**同じ時刻・同じ id** を持つこと（順序が非決定になる）は起きてはならない（拒否されるか決定的に扱われる） | (ts,id) 衝突の排除／決定性 | 小スコープ全順序 | 未実装（静的: 現状 衝突時に非決定＝要不変条件追加） |
| MC-SUP-CANCEL | §2.7・R-S5・R-C2 | supersede と cancel が**どんな順序で到着しても**、有効集合は正しく、出来高の二重計上も誤った脱落も起きない | 全到着順で有効集合正・EV_abs 整合 | ノード2・イベント≤4 | 未実装 |

---

## C. DENY（書かない／否定プロパティ）

「やること」だけでなく「**意図的にやらないこと**」も記録する。これが無いと正しい挙動を誤ってバグ報告してしまう。否定プロパティは**到達 witness 必須**（条件を実際に踏んだ上で『黙る』ことを確認）。

| ID | 根拠 | 人間がレビューする一文 | 種別 | 現状 |
|---|---|---|---|---|
| DN-MONOTONIC | P5・R-E4・R-E3 | 達成率は**下がることがある**（手戻り・上方再見積・新規合意）。これは正直な信号でバグではない。ゆえに『増える一方』とは**決してテストしない** | テスト禁止 | 規約 |
| DN-IMPL-DATES | P8・§7 | 完了予測・プロジェクト導出完了・バッファの**絶対値**は実装依存。MODEL 不変条件として固定しない（実装特性の golden 固定は別枠で可） | テスト禁止（別枠 golden は可） | 規約 |
| DN-CAL-SILENT | §7（暦の穴） | 将来の休暇を入れ忘れても、システムは**警告しない**（既定1.0で楽観計算）。意図した穴で暦保守は人間の責任。検知を期待せず『黙る』ことを確認する | 否定（witness 付き） | 未実装 |
| DN-DRIFT-SILENT | §7（ドリフト） | 記録上の c と実態のズレ（記録1.0だが実は休んでいた等）は**検知しない**。予測も凍結スロットも記録値から導くため。認識は人間の責任 | 否定（witness 付き） | 未実装 |
| DN-MATURITY-BLIND | §7（前倒し） | 前倒し見積を早期合意するとカバレッジは回復するが、それが情報の薄い見積か成熟した見積かを**システムは区別しない**。成熟度の軸は持たない | 否定（witness 付き） | 未実装 |

---

## clause → property 被覆（未 bound は可視ギャップ）

第一版でカバーした clause: A 系=I1・I3・I4・R-U8・R-U10・R-U12・R-C2・R-S5・R-S7・A6・P1・P3・P4・P5・P6・§2.5・§2.7・§2.8・§3・§7。外的妥当性: 受け入れシナリオ `cancel-task-midway`（PR-CANCEL 系と対）。**未 bound（今後追加）**: R-T3 過負荷・R-T4 期日超過・R-T6 バッファ・R-S3 スラッシュ・R-S6 スケジュールカバレッジ de-rate・R-S8 実行カバレッジ・R-C3 キャンセル孤児・R-D2 辺ポリシー既定・R-E2b 見積作業のノード化・R-U13 未合意完了警告。これらは可視ギャップとして残し、批准後に増分で埋める。

## 次手（批准が通ったら）

最小パイロット（ログのみ・実装変更ゼロ）: **PR-EVPCT-RANGE / PR-I1-ROLLUP / PR-CANCEL-EXCL / PR-AC-ROLLUP / PM-ORDER-INV** ＋ 決定打 **PR-DONE-LOCK★**（現実装が赤になることを実走で確認）。生成器2つ（イベントログ＋容量 c）を敵対的構成・一級レビュー対象として用意する（[moira-verification.md](../.kiro/steering/moira-verification.md)）。
