{
  "name": "moira-backend",
  "private": true,
  "type": "module",
  "description": "Moira S4 minimal backend — append-only event log → fold derivation → thin read interface",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "snapshot": "tsx src/read/snapshot-cli.ts",
    "serve": "tsx src/read/http.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "fast-check": "^4.8.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  }
}
