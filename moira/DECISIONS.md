# Moira 意思決定ジャーナル

> ブラッシュアップの過程で人間が下した確定済みの分岐、未解決の論点、次の一手を記録する。
> MODEL.md の §6 来歴(版の主要転換)とは別に、**どの分岐をなぜそう決めたか**の運用ログ。
> 更新は moira-model-update スキルが解消した分岐をここに追記する。

## 確定済みの分岐(原セッション v1–v9)

- **重みテーブル → 単一式に溶解。** EV はどの階層でも「完了 ÷ 合意済み」。状態に重みを割る発想を廃止。
- **spec 留保率 → ノード化で溶解。** 仕様作業は独自見積を持つただのノード。
- **能力トレース → 廃止。** スキル・習熟度をモデル化しない。割当は人間が外から与える入力(R-U6)。
- **見積 = 合意状態を持つ提案。** `proposed→agreed`、合意は人間のみ(A5/R-U4)。粒度はタスク/feature ノード単位(仕様方法論の成果物承認ゲートと一致——cc-sdd では tasks approved——ボトルネック回避;v16 Phase 0 0c で方法論非依存に一般化)。
- **EV は合意済み見積のみで算出**、未見積は見積カバレッジで可視化(P2/R-S4)。
- **辺ポリシー既定 = implemented**、かつ `implemented ≡ 実装完了 ∧ 仕様FIX`(R-D2)。
- **「見積の見積」→ 規則の底打ちでなく人間のコミット判断(P0 第4項)に還元。**
- **スコープ削除の自動化はしない**(R-T4:アラートのみ、判断は人間)。

## 確定済みの分岐(v9 → v10 ハードニング:独立敵対レビュー)

> 著者が敵対者を兼ねた v1–v9 と異なり、独立した敵対者(moira-adversary)＋独立採点者(moira-gate-judge)が
> 攻撃し、自己レビューが見落とした構造的綻びを是正(確定基準は独立採点者の残存 Critical/Important = 0。
> ラウンド数は規律上、自賛として記録しない)。ユーザーが下した分岐(F1/F2/F3 はレビュー作業上の便宜ラベルで
> MODEL 用語ではない):

- **F1 無限後退の停止 → v9 維持(人間判断)。** ただし「AI が外部所与に接地した入力から提案するので空虚でない」を §2.3 に明示。入力後退は構造的に接地、深さは人間判断で有限化(構造保証とは主張しない)。
- **F2 supersede の EV basis → A:累積保持 + R-S5 明確化。** 旧ノードの EV_abs は**累積稼得 basis**に残す(cancelled とは別)。ただし**カバレッジ(P2)・現行進捗の分母=現行有効集合では二重計上しない**。累積稼得(EV_abs、supersede 込み)と現行進捗(現行有効集合の EV%)を basis ごと分離(§2.7/P2)。
- **F3 金額通貨 → 撤去(最小性)。** A6 をアテンション時間の単貨に、R-U10 調整。消費する UC が無いため。
- **PV/ベースライン → ユーザー判断で PV を扱う**(PV なしに EVM 管理は成り立たないとの判断)。ユーザーの当初直観「PV は見積合意のたびに確定」を起点に EVM ベースライン(PMB)を正規化したが、レビューで**予算は合意時に確定するものの「合意時に全部 FIX」は不正確**と判明し撤回:正しくは**予算=合意時・スロット=初回スケジュール載せ時に、既存 `transition` の凍結属性としてログ記録**(純導出でなく記録)。完了で施錠。SPI は部分集合指標と認め R-S6(スケジュール・カバレッジ)新設。

### v10 で是正した主な構造的綻び(自己レビューの見落とし)
- §6「6次元満点」自己採点・§7「すべて完了」・§5「未処理ケースが残らない」(v9 の文言)・§5 が参照していた未定義の UC1–3 → v10 で撤回/正直化。
- EV の次元破綻 → EV_abs(絶対量)/EV%(達成率)の二形分離、SPI/CPI を予算次元に整合、全要件へ伝播。
- I1 が実装ノード誕生で一時的に破れる → 「合意済みの子のみ Σ、未見積はカバレッジ低下」で整合化。
- §2.5「実装完了∧仕様FIXは必然に同時」→「判定であって保証でない」に(P5 と整合)。
- I7 を最小性で降格(R-D7・I2 へ)。暫定割当の上書き機序・R-D4 述語評価・R-S3 誤発火抑止 ほか。

## 確定済みの分岐(v11 → v12:α_i パラメータ化)

> ユーザーが「人間は 0.2MD/日〜1.0MD/日 とまちまち」と指摘。moira-model-update スキルの
> 敵対的レビューループ(adversary ×3 → 再反論 ×1 → gate-judge)を経て v12 に確定。

- **α_i を公理(A4)レベルで MODEL に導入。** v11 の固定容量 1.0MD/日 を α_i ∈ (0, 1.0] にパラメータ化。α_i なしでは P7 が楽観的スケジュール、R-T3 が偽陰性を生む。P0 で運用吸収可能だが、その場合システムの導出指標が意図的に不正確になり P0 の精神に反する。
- **α_i は外的制約入力であり、廃止済みの裁量パラメータ(重み・留保率・スキル)とは質的に異なる。** L40 を修正し区別を明記。廃止パラメータは構造的代替で還元可能だったが α_i の機能を還元する代替は存在しない。
- **α_i はプロジェクト構成入力として A2 のイベントログとは別次元で管理。** R-U14 で変更追跡を要求。4 イベントへの追加は行わない。
- **α_i の定義域は (0, 1.0]、既定値 1.0MD/日（後方互換）。** α_i = 0 は割当解除で表現。上限 1.0 は標準稼働日容量(A6)に基づく。残業は AC で反映、計画容量には織り込まない。
- **複数プロジェクトの Σα_i 整合はスコープ外。** 本モデルは単一プロジェクト視点。

## 確定済みの分岐(v12 → v13: 暦/容量の単一 c 一般化)

> 「暦(祝日/休暇/稼働日)と α_i の関係」の対話から派生。moira-model-update の敵対ループ
> (adversary ×3 + fact-checker → ユーザー裁定 FORK → 再challenge ×2 → gate-judge)を経て v13 に確定。

- **α_i スカラー → 単一の日次容量 c(i,d) ∈ [0,1.0] MD/日 に一般化。** 祝日・休暇・部分稼働日を正直に time-phased PV へ反映するため。c は理由付き append-only 改定の**区間定数**(暦日ごとの任意関数ではない)。
- **【ユーザー裁定 FORK】容量の表現形 → 単一 c(α は導出 view)を選択。** 敵対者が「二因子 α×暦 はどの導出も積しか読まず冗長、かつ非一様な日次配分で a>1 を要求して破綻」と示し、AskUserQuestion で「単一 c／二因子／v12 維持」を提示。ユーザーは**単一 c** を選択。契約レート α_i は「c の reason=契約 成分」の**名前付き導出 view**(SPI/EV%/CPI と同型)へ降格——v12 で公理化した α_i の一級位置づけを撤回。
- **R-S7 を cause-agnostic 化。** 陳腐化トリガーを「割当変更」から「生きた予測の完了が凍結スロットと乖離(原因＝割当/c のいずれでも)」へ一般化。これにより v11→v12 の「α_i にトリガー条件は存在しない」開示を**撤回**(記録された c 変更が凍結ベースラインを無効化する場合を検知。記録と実態の drift は依然非検知＝P0+R-U14)。
- **c は slot 次元/PV(t) 時間配分に効き、予算総額には効かない。** c=0 は定義域内(祝日/休暇)。恒久離脱は割当解除(§2.4)、一時的不在は c=0——境界は P0(モデルは規定しない)。
- **二層データを正直に開示。** A2「唯一のデータ＝4イベントログ」はノードのワークデータを指し、構成入力 c は別層(R-U14 履歴)。v12 で α_i について既に成立していた区別の一般化。
- **残余開示(§7)**: drift 非検知 / 暦未指定日の楽観既定 / 広範 c 変更のスロット陳腐化洪水 / 過去予測再現の valid-time / 恒久 c=0 の二経路。いずれも P0+実装に委ねる。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0。新公理・新イベント・新状態・新要件番号なし(R-S7/R-T3 は意味論拡張)。

## 確定済みの分岐(v13 → v14: 警告の持続・消滅意味論)

> Moira 実装の画面アーキ(行為面=decision インボックス採用)検討中に独立敵対ゲートが MODEL 本体の穴を検出。
> moira-model-update の敵対ループ(adversary ×3 + fact-checker → ユーザー裁定 FORK → gate-judge)を経て v14 に確定。

- **警告は「現在の導出状態に対する述語」**であり、条件が真である限り可視ギャップに残り、**条件を偽化する入力(4イベント追記 または c 変更; R-S2)**が起きた時のみ消える(§2.1 新設条項)。歴史的事実そのものでは判定しない(R-U12=現行 latest-wins 値、P5 at-risk=implemented 再到達前)。
- **「acknowledge(イベント無し)では消えず、commit(イベント)で消える」。** 人間が見て受容し何もしないのは正当な**非コミット**で、警告を消さない(true な信号の隠蔽は §0/P0 に反する)。R-U13 事後合意・R-C3 辺操作・R-T4 スコープ削減は commit=イベントゆえ消える(整合)。
- **【ユーザー裁定 FORK】confirm/受容型と S/N の扱い → α(acknowledge 可変状態を持たない)を選択。** 具体ケース(夏季休暇で R-S7 が100スロット陳腐化/難研究の R-S3 thrashing/R-T4 再交渉中)付きで α/β/γ を AskUserQuestion に提示。ユーザーは **α＋「β 上位互換パスを §7 に開示」**を選択。
  - α: 警告は真の限り可視ギャップ会計に残す。提示層は「据え置き・既知/新規」を区別(畳む・淡色化)してよいが**会計から除いてはならない**(falsifiable な線=P0)。
  - β(将来の上位互換): 受容の監査証跡(誰がいつ何を据え置き受容したか)が UC 化したら、c(R-U14)と同型の**追記専用 acknowledgment 層**を後付けし、活性述語を「条件が真 ∧ 最終条件変化以降に未確認」へ拡張(ノードのワークデータでない別層ゆえ A2 非違反)。
- **R-S7 の「確認」=据え置き受容(非コミット)で陳腐化標識を解除しない。** 乖離は variance として正直に残す(EVM 本義;fact-checker が PMI/ANSI-EIA748 で CONFIRMED)。消滅は理由付き再ベースライン(イベント)か生きた予測の再収束のみ。一括確認=提示 salience、一括再ベースライン=イベント群。
- **「持たない」根拠の訂正(自己の過去前提の撤回):** acknowledge を持たない根拠は **A2 ではなく最小性**。acknowledge はノードのワークデータでないため A2 は直接禁じない(c と同じく別層たりうる)——v14 ループ中に敵対者が指摘し訂正。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0。新公理・新イベント・新状態・新要件番号・新原理番号なし(既存条項の意味論明確化)。fact-checker NO_OBJECTION。

## 確定済みの分岐(v14 → v15: スケジュール・バッファのモニタリング)

> ユーザー全体要件「プロジェクト全体で管理しているバッファの残量をモニタリング」を正典化。
> moira-model-update の敵対ループ 3 ラウンド(各 adversary ×3〔V1–V6〕+ fact-checker、計12敵対派遣+3事実検証 → ユーザー裁定 FORK ×3 → gate-judge)を経て v15 に確定。

- **当初案(CCPM 概念を §3 に導出追加)を全面棄却・著者が過剰主張を撤回。** Round 1 で3敵対者が独立に「CCPM バッファ(安全余裕除去の産物)≠ Moira スラック」「5項目すべて P7/R-S7/R-T4 と冗長」「§5 留保率全廃と矛盾」「クリティカルチェーン同一性が P8 非決定性で不安定」を指摘。著者は反証でなく撤回し、唯一の実構造ギャップ＝**R-T4 が参照する外部期日の入力未形式化**に再フレーム。
- **【ユーザー裁定 FORK (a)】バッファ正典化の形 → 「二参照点＋規律付き導出量」を選択。** 期日(外部制約・硬い上限)・目標日(計画完了の参照点)を構成入力として形式化し(c と同じ第二層・R-U14 同型・プロジェクトレベル単一)、新要件 **R-T6** でバッファ残量 = max(0, 期日 − 導出完了)・消費率 = clamp₀¹((導出完了−目標日)/(期日−目標日)) を導出。
- **L47「裁量パラメータ不在」非抵触の論証。** バッファ残量は期日(外的事実=契約/市場制約、c 寄り)と P7 導出完了のみに依存し、核心会計(EV/SPI/CPI/PV/スケジュール)を一切読まず調整しない;目標日は消費%の再基準化のみ。全廃した留保率は EV/カバレッジに織り込まれ進捗を歪めた**暗黙の乗率**だった点で質的に異なる(明示的参照点 vs 暗黙ノブ)。
- **偽の安心(P0)の防止を必須化。** R-T6 はバッファ残量を**スケジュール・カバレッジと対読みで de-rate**することを必須とする(SPI の R-S6 と同型;未割当バックログ大で D_pred が楽観的に過小→偽の余裕、を封じる)。
- **R-T4 と R-T6 を表裏に整合。** R-T4 = 期日超過時の警告、R-T6 バッファ残量 = 超過前の監視、同一の導出完了の表裏(超過量はバッファ残量が 0 にクランプされた後の同量)。
- **【ユーザー裁定 FORK (b)】粒度 → プロジェクトレベル単一を選択。** 期日・目標日は各1つ。ノード/フェーズ別バッファは §7#12 に拡張として開示(大規模でのフェーズ別期日は認識するが最小性優先で見送り)。
- **【ユーザー裁定 FORK (c)】工数軸(MD/MM・BAC×想定生産性) → 「§7 に拡張開示・今回は時間軸のみ」を選択。** Round 3 で3敵対者+fact-checker が「想定生産性(=計画CPI)を BAC に掛ける乗率は留保率と算術同型で L47 抵触」「工数上限は L33(予算総額不変)違反」「EAC=BAC/CPI は §3 既存 EAC と二重定義・CPI 序盤不安定」「MM は Brooks の『神話の単位』(fact-checker CORRECTED)で A6 と緊張」と収束。ユーザーは工数軸を正典に入れず §7#11 に**認識済みの未採用拡張**として開示する判断。**忠実な実現路**(将来 UC 化時)＝想定生産性をパラメータ化せず工数枠(envelope)を MD で直接宣言する外的事実入力＋ latest 駆動 EAC、を §7#11 に記録。
- **isBuffer ノードは不採用(A1 違反)に解消。** PROTOTYPE-EVALUATION §5.5 の「設計判断として留保」を解消(バッファは宣言でなく二参照日付＋導出完了からの導出)。UI-DESIGN-BRIEF §0#4・関連表も v15 整合に更新。
- **CCPM の正直な位置づけ。** バッファ*監視*の着想(消費の早期可視化)は借りるが、バッファ*生成*(安全余裕の除去・集約)・ゾーン閾値(GREEN/YELLOW/RED)は不採用。fever 風の消費%×進捗% 可視化は提示の自由(P0)。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0(PASS)。新公理・新イベント・新状態・新原理番号なし。新要件は **R-T6 一件**(既存の P7 導出完了＋二参照日付の上のバッファ監視)。fact-checker は EAC=BAC/CPI の標準性・management reserve の PMB 外性・留保率と MR の区別・CPI 序盤不安定(Christensen&Heise)を裏取りし、「BAC×想定生産性=工数バッファ」が COCOMO でないこと・MM が Brooks 神話単位であることを CORRECTED。

## 確定済みの分岐(v15 → v16: 実行カバレッジの正典化・獲得ルールの却下)

> ユーザー指摘「二値 EV(完了まで0→満額)だと執行中の進捗把握が遅れる」を起点。
> moira-model-update の敵対ループ(round1 P-EARN 却下 → ユーザー裁定 FORK → round2 P-EXECCOV → R-U8 反証の
> 再反論 → gate-judge PASS)を経て v16 に確定。思考実験 = `moira/thought-experiments/11-progress-granularity-earning-rule.md`。

- **当初案 P-EARN(`implementing` で固定割合 F の partial EV を稼ぐ獲得ルール)を却下。** Round1 で3敵対者が独立に
  「F は状態→重み写像で R-U8『no state-weight table』/P1『重みテーブルなし』該当」「EV に折り込む乗率で L47/§7#11
  (留保率と算術同型)抵触」「分解(P1)で除去可能」「PV 非対称・下方再見積で EV%>1 等の未処理多数」を指摘。
  fact-checker は 0/100・50/50・20/80 が標準 EVM の客観手法と NO_OBJECTION で裏取りしたが、Moira の L47/R-U8 は
  標準が保持する重み表を意図的に溶かした分**より厳格**ゆえ支えにならないと確認。著者は強行せず却下(accuracy-first)。
- **【ユーザー裁定 FORK (canon-home)】「執行中の集約可視化」の実現 → 正典 R-S 要件を選択**(他候補:実装/UI のみ・
  §7 開示)。partial EV を使わず核心会計を読み変えない別建てオーバーレイ **実行カバレッジ executionCoverage** を
  新要件 **R-S8** として追加。
- **executionCoverage = count-based coverage(scheduleCoverage 同型)。** = |合意済み有効葉のうち implementing| /
  |合意済み有効葉|(ノード数比率・分母0→0)。述語が「scheduled?」→「implementing?」に変わっただけで scheduleCoverage
  (R-S6)と構造同型。**予算に状態別乗率を掛けず状態述語でノードを数える**ゆえ R-U8 の射程外(R-U8 は EV% 稼得の
  state-weight を禁ずる)、**EV_abs/EV%/PV/SPI/CPI の式に一切触れない**。
- **R-U8 該当(FORK1)を scheduleCoverage 構造同型で反証 → 別 adversary の再反論で健全と確認(round3)。** でなければ
  R-S6/P2 自体が R-U8 違反になり正典が自己矛盾、という対称性が決め手。
- **de-rate 規律(必須)。** execCov は仕掛中の*量*であって出来高ではない(implementing に居座るだけのタスクも算入)。
  **EV% との算術和=全体進捗としての提示は禁止**(次元が異なる;R-S4/R-S6 同型)。実際の前進は R-S7 乖離・P6 リード
  タイムと対読み。EV%・estimateCoverage と三者併置。
- **合意済みのみ(execCov 独自条文)。** 未合意の implementing 葉は分子・分母外で estimateCoverage の可視ギャップ
  (P0)に現れる(EV% の R-U8 と思想同型だが条文は別)。
- **残余開示(§7#13)**: count-based ゆえ effort 非加重(粗さは scheduleCoverage と同性質・予算加重は R-U8/L47 抵触
  ゆえ意図的に不採用)/ 忠実度は implementing 記録に依存(ready→implemented スキップは直接 done)/ 生きた状態ゆえ
  変動的(凍結の scheduleCoverage より)・値は平準化非依存だが implementing 遷移のタイミングは運用依存。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0(PASS)。新公理・新イベント・新状態・新原理番号なし。
新要件は **R-S8 一件**。R-U8/L47/P1/EV_abs/SPI/CPI/PV は不変。

## 確定済みの分岐(v16 Phase 0: 正典分解の前提整備・編集的ハードニング)

> MODEL v16 を CQRS で読 spec×10／書 skill×14 に分解する(`.kiro/steering/roadmap.md`)前提として、
> 分解がモデルに触れる論点を先に決着。**版番号は据え置き(v16)**——導出・数式・状態機械を変えない編集的明確化と
> 方法論非依存化に限る。moira-model-update 独立敵対ゲート(adversary×3〔V1–V6〕→ 著者パッチ → gate-judge)。

- **【ユーザー裁定 FORK 0a】非spec/未仕様タスク vs A1 → 「A1 は既に包含」を選択。** A1 直下に射程注釈を追加:「spec」=遂行され出来高(EV_abs)を生む**あらゆる作業単位**(運用/バグ/ad-hoc も feature ノード)。フェーズ周期の省略は §2.6 子ノード展開の**分解深さ**(§2.1#4/P0)であって lifecycle 状態(§2.5)の省略ではない。**バッファ等の導出会計量は「作業単位」に含まれない**(isBuffer 却下〔§5〕と整合)。
- **【ユーザー裁定 FORK 0b】チケット射影機構 → 「不変条件のみ・メカニズムは実装」を選択。** A1 の帰結として射影の性質を明示(新公理ではない):(1) 読み出し専用=チケット→ノードの直接逆書き込み経路なし(外部変更は取り込みアダプタ経由の4イベント**再発行**のみ;A2/R-U2 の帰結)、(2) 全射でない=射影対象は実装選定。**多重度(ノードあたりチケット数・対象システム・API)は実装責務**で MODEL は規定しない(マルチシステム同時射影を許容)。
- **【ユーザー裁定 FORK 0c】spec 単位の汎用化 → 「参照を一般化」を選択。** cc-sdd 固有参照(A4・§2.2・§2.3・§2.6・R-U5)を方法論非依存に一般化し、cc-sdd を検証参照例として括弧内保持。「仕様方法論」を操作的に定義(成果物を段階的に作成・人間承認する構造化プロセス;他方法論は各成果物承認ゲートを §2.2 ノード合意へ写像)。検証は cc-sdd に対して行われた旨を P2 同型に正直開示。
- **【ユーザー裁定 FORK 0d】用語/NAMING → 確定。** 累積稼得→**累積EV**(ユーザー選好)、**コミット行為**(UI 語、正典語=コミット判断/コミットメントを伴う判断と同義)、**実行カバレッジ**(R-S8)を NAMING §7 正式用語表に確定。MODEL 本文の累積稼得残存も累積EV へ同期。
- **0e 正典陳腐化解消(確定ナレッジ更新)。** UI-ARCHITECTURE.md 被覆表 v14→v16(R-S2 導出 11→13=実行カバレッジ R-S8＋バッファ R-T6 追加・R-S8 三者対読み host=spec-value・累積EV 表記統一)、validation-scenarios.md v10→v16(S4 に R-S8/R-T6、S5 を c(i,d)＋R-T6、固有判断 S14〔R-S8 居座り/スキップ〕・S15〔R-T6 境界条件〕新設)、NAMING.md §7＋MODEL 連動条項。

- **敵対ゲートが検出し解消した Critical 3 件:** (C1) A1 射程拡大が §5 isBuffer 却下根拠を自己矛盾化 → A1 を「遂行され出来高を生む作業」に限定し isBuffer 却下を導出会計量の二重化として再接地。(C2) A1 射程例示 `implementing→accepted` が §2.5 状態機械に非在 → 省略対象はフェーズ子ノード展開であって lifecycle 状態でないと明確化。(C3) 射影「高々一対一」がマルチチケットシステムで破綻＋A1 精神と矛盾 → cardinality を撤去し多重度を実装責務化(0b と整合)。Important(コミット行為↔判断整合・DECISIONS 陳腐化・累積稼得残存・射影の公理インフレ・NAMING 命名原則の §0 抵触・NAMING 同期欠落・validation 固有シナリオ欠如・UI-ARCH 三者 host・A4 参照過広・R-E2b 引用)も是正。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0(PASS)。版番号据え置き(v16)。新公理・新イベント・新状態・新要件・新原理番号なし。

## 確定済みの分岐(v16 見積二面性の明確化: scenario 起点・編集的・版番号据え置き)

> 受け入れシナリオ `.kiro/scenarios/units/estimate-spec-proposed.md` が見積の内部表現の三者乖離
> (原案=proposed/agreed 2行／MODEL=est 独立ノード／参照実装=作業ノード属性)を炙り出し、
> moira-model-update の敵対ループ(R1: adversary×3 → 著者パッチ → R2: adversary×2 → 著者再パッチ
> → ユーザー裁定 FORK → gate-judge PASS)を経て確定。シナリオ層(人間=妥当性)と MODEL 整合層
> (専門家エージェント)の役割分担で、人間が委譲した内部表現を専門家ループが裁定した事例。

- **敵対ゲートが検出した MODEL 内部の未解決点(初回著者主張を撤回):** R-E1「見積をノードで表現(shall)」が
  (読みA)見積*作業*の別ノード義務と(読みB)見積*値*の担体の二読みを許し、R-E2b「軽微なら畳む(ノード化しない)」と
  **文面矛盾**。proposed/agreed 状態の所在(被見積ノード vs est ノード)・est ノードの木所属/lifecycle/I1 寄与が
  **未規定**。かつ「S4 参照実装は既知簡略化」は**未文書化の過剰主張**だった。著者は「MODEL は変更不要で内的整合」を撤回。
- **【ユーザー裁定 FORK】見積の正典表現 → ハイブリッドを選択**(他候補:常に作業ノード属性／常に独立 est ノード)。
  - 見積*値*＋`proposed`/`agreed` 状態は**被見積(フェーズ/作業)ノード**に乗る(R-E1/§2.2;合意 `transition` は当該ノードを名指す)。
  - 見積*作業*の est ノードは**相応に重いときのみ R-E2b でノード化**(軽微なら畳んで cost;無条件には要求しない)。
  - 粒度はフェーズ別(est(req)/est(design)/est(tasks)/est(impl))、est(impl) は tasks 生成 decompose への内包禁止=分離(R-E1b)。
- **著者パッチ(編集的明確化):** R-E1 の義務範囲を明確化、R-E2 主語を「ノード化作業でも被見積ノード直接でも被見積ノードに
  値を載せ当該ノード上で合意」へ同期、§2.2 に状態の所在、§2.3 に二面性＋連鎖の最大展開/縮退、R-E1b を分離規定へ、
  §2.6 木図に est 省略注、§7#14 に二面性・非実装ノード lifecycle の A1 既存論点・ロールアップ親 estimateState・
  S4 fixture 簡略化・下流 spec 同期(別タスク)を開示。
- **健全と裁定された反証2件:** (i) est ノードの `implemented`≔実装完了∧仕様FIX が非実装作業に当てはまらない件は、
  A1 が運用/バグ等の全非実装ノードに同一 lifecycle を課す**以前からの既存論点**で、本パッチは est を必須→任意にして露出を
  むしろ減らす(§7#14b で開示)。(ii) 版番号据え置きは、R-E2b が元から est を任意にしていたため意図は不変・R-E1 の矛盾文言を
  意図に合わせた明確化(新 A/I/P/R 番号なし・数式/状態機械不変、v16 Phase 0 同型)。
- **下流同期(別タスク):** `moira-ingestion-adapter` spec R3/R4「est を候補ノードとして提案」は「R-E2b に従い重い場合に
  ノード候補化」と読むべき同期対象(本スキルは MODEL と派生文書のみ確定;§7#14f)。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0(PASS)。版番号据え置き(v16)。新公理・新イベント・新状態・新要件・新原理番号なし、導出・数式・状態機械不変。

## 確定済みの分岐(v16 → v17: 前倒し見積と暫定割当の状態保持遷移)

> 受け入れシナリオ駆動レビュー(kiro-scenario)が3シナリオ単位(discovery/estimate/assign)の凍結時に2件の正典ギャップを ESCALATE。moira-model-update の敵対ループ(R1: adversary×3 → 著者パッチ9編集 + fact-checker×1 → R2: adversary×3 → 統合パッチ → R3: adversary×2〔参照実装照合〕→ 精度パッチ → gate-judge PASS)を経て確定。シナリオ層(人間=妥当性)と MODEL 整合層(専門家エージェント)の役割分担で、人間が委譲した正典ギャップを専門家ループが裁定した2例目。

- **ESCALATE 3件中1件はダウングレード。** decompose による既存子ノードの見積改訂は §2.8(decompose=子と見積を設定/改訂)・§2.3(est が見積を産み decompose で記録)で**既にカバー済み**ゆえ正典ギャップでないと判定(誤った escalate を回避=accuracy-first)。残2件を裁定。
- **【ユーザー裁定 Option A】ESCALATE 2件を本スキルで解決→全 agreed を選択**(他候補:今 agreed 確定し後でまとめて model-update／draft 維持し model-update 後に再確定)。
- **【ユーザー裁定 FORK①】見積入力連鎖 → 「理想形(前倒し許容)」を選択**(他候補:厳格維持/ハイブリッド暫定見積)。§2.3 の入力連鎖(est(design)←requirements 等)は**最大情報の理想形でありハード前提ではない**——下流成果物の存在前に brief から req/design/tasks を前倒し一括見積してよい(通常の `proposed`、信頼度属性は持たせない)。R-E3 のトリガーを「上流入力の成熟」へ拡張。前倒しは AI 提案であって新たなコミット判断を増やさず §0 五判断は不変(合意ゲートが有限化するのは agreed 数であって proposed 量ではない)。
- **【ユーザー裁定 FORK②】暫定割当の表現 → 「状態を保つ遷移(to=現状態)を正典化」を選択**(他候補:pending→ready へ進める/別表現・第5イベント)。assignee は状態機械の一部でない**付帯記録**(§2.8)ゆえ、状態を保つ `transition`(to=現状態)に載せる。**状態機械(§2.5)には辺を足さない**(自己ループはイベント側の事実;FSM 論「自己ループは明示要素のみ」を fact-check で確認のうえ付帯記録は状態機械要素でないと整理)。§2.8 に一次集約し §2.4/§2.5/§5/R-T5 を参照に縮約。根拠を I5 から §2.8 付帯記録性へ統一。
- **ラウンド2で参照実装照合により検出・解消した Critical:** (i) R-E3 の無限定拡張が**完了ノードの EV_abs 非単調脱落**を生む(ev.ts は agreed のみ算入・fold.ts は再見積で proposed 化)→ R-E3 を**incomplete 限定**、完了は I4 施錠・完了後変更は supersede(§2.7)。(ii) **解除(unassignment)が現行符号化(types.ts `assignee?:Actor`、none センチネル無し)で表現不能**→「解除＝被割当者なし名指し」の断定を撤回し §7#16 開示事項に格下げ。(iii) 自己遷移規範の4-5重重複→§2.8 一次集約。
- **ラウンド3で検出・解消(精度・参照実装照合):** 未完了 agreed→proposed 復帰が **EV% を上方**へ動かす(分母 Σagreed-latest 縮小;ev.ts で 0.50→1.00 実証)→ R-S4 de-rate＋P2 対読みの開示で閉鎖(機構は既存)。§2.8「提示は変化点のみ」を提示の自由へ降格・「確定・更新」を「確定(初回のみ);更新は理由付き再ベースライン §3」へ fold.ts 整合・表の I5/付帯記録根拠を分離。fold/leveler/PV/execCov と MODEL の「進捗不動・スケジュール可動」分離が一致を確認。
- **撤回・訂正(自己の過去主張含む):** 旧 §2.3「入力後退は構造が強制」→「最深の外部所与接地のみ構造強制」。「合意ゲートが提案量を有限化」撤回。「解除＝被割当者なし名指し」撤回。「提示は変化点のみ」規範→提示の自由。「自己遷移は状態機械に辺を足す」→「イベント側の事実・状態機械不変」。
- **fact-check(全 CONFIRMED, hedge付):** FSM 自己ループは明示要素のみ／標準 EVM ベースライン予算固定／agreed→proposed 巻き戻しは標準外(到達文献では確認されず=全称否定を避け hedge)。proposed↔agreed 往復と前倒しは**標準 EVM 外の意図的拡張**と §7#15(d) に正直開示。
- **残余開示(§7#15/#16):** 前倒し合意の成熟度を P2 は区別しない(無警告楽観)／前倒し提案量は提示の責務／「暫定割当済み・スロット未凍結」は準コミット領域(専用可視化なし)／解除の none センチネル符号化は未解決。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0(PASS;参照実装 ev.ts/fold.ts/leveler.ts/coverage.ts/types.ts と照合)。新公理・新イベント・新状態・新原理番号・新要件番号なし(既存 R-E1/R-E3/R-T5 の意味論明確化と §2.8 への付帯記録専用 `transition` の明示)。**下流 spec(moira-core/moira-schedule)の R-T5「into active state」文言同期は別タスク(§7#14(f))。**

## 確定済みの分岐(v18 → v19: 指名レビュー担当 `reviewer` を付帯属性として追加)

> 受け入れシナリオ駆動レビュー(kiro-scenario)で人間(外的妥当性オーナー)が「Claude が要件定義を作成し `implemented`(レビュー待ち)になったとき、**次にレビューするのは誰か(太郎)**を spec-value/schedule の両画面で per-node に見たい。担当が Claude のままなのは嫌」と要望。現行モデルは assignee=作業者・単一、人間レビュー待ちキューは `implemented` の **actor 非依存**導出で「誰がレビューするか」を持たない。

- **【ユーザー裁定 FORK・2巡】** 「レビュー担当をどう表すか」を2案で敵対ゲートにかけた:
  - **Option A=assignee を ball-holder(現在の手番)へ再定義 → 却下。** moira-adversary×3 が一致で破った——(i) `leveler.ts:44-52` の schedulable は `assignee≠null∧agreed∧estimated` を **lifecycle 無関係**に平準化対象とするため、`implemented` で assignee を太郎へ張り替えると完了作業の見積分、太郎の c を**誤消費**し P7/R-T3/R-T6/R-S7 を汚染、(ii) humanReviewQueue は actor 非依存ゆえ張り替えても**太郎が出ない**、(iii) §2.1#2「割当は自動化しない」と『implemented で自動張り替え』が抵触、(iv) agreed 単位の『担当(作業者)≠玉(手番)』分離と矛盾。
  - **Option B=reviewer を assignee と別の付帯属性で持つ → ユーザー選択・確定。**
- **R2 最重量 Critical「視点フィルタで足り reviewer 冗長(agreed 単位 L326 が MODEL 変更不要と裁定済み)」を反証→同一ラウンド再反論=SOUND:** 自分フィルタ(viewer 依存・自分の分のみ)と per-node 指名レビューア(全員に・事前に可視)は**別 UC**。L326 は『自分フィルタ』限定で per-node 指名を含まず(L319 が後者をキュー導出の穴と明示)。`humanReviewQueue` は NodeId のみでレビューア datum 無し・視点フィルタは viewer 依存ゆえ per-node 指名を全員に出せない＝reviewer は実装上必要(再反論者が queues.ts/leveler.ts/L319/L326 の現物で確認)。
- **中核の確定:** `reviewer`(implemented→accepted を行う指名された人間)を `transition` の付帯属性として追加(§2.4/§2.8/R-T5)——単一・latest-wins・**人間限定(書込み層規律;fold は assignee 同型に非強制)**・assignee と別軸で **leveler/EV/PV/coverage を消費しない**(レビューは見積を持つ作業単位でない;§7#18)。
- **ゲート指摘の解消:** (i) 五判断の数=reviewer 指名は §2.1#2 割当に含め**5維持**(承認の*行為*は品質確認で判断でない・agreed L325 と整合)、(ii) 指名 reviewer と実承認者(accept actor)の不一致は**許容**(構造強制せず;強制 UC 発生時に I6 同型を別途検討)、(iii) leveler 非消費を `leveler.ts` 現物で確認、(iv) 最小性=別役割の単一指名で UC ありゆえペア却下と異なる、(v) §2.1#2「割当」拡張が agreed 単位 `assign-spec-provisional`(割当=作業者) の所管に **additive** に触れる旨を §7#18(g) 開示。
- **撤回:** Option A(assignee=ball-holder)を leveler 汚染等で却下し §6 に明記。
- **波及同期:** `types.ts`/`fold.ts`(reviewer 属性)・NAMING を本コミットで同期。spec(core/schedule/surface)の reviewer 要件化・参照実装フロントエンドの reviewer 表示・受け入れシナリオ単位への反映は別タスク。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0(PASS;参照実装 leveler.ts/derive.ts/queues.ts/fold.ts/types.ts・agreed 単位 L319/L325/L326 と照合)。新公理・新イベント・新状態・新原理番号・新要件番号なし(R-T5 を reviewer 記録へ拡張・付帯属性1つ追加ゆえ版上げ)。全 46 backend テスト緑。

## 確定済みの分岐(v19 内訂正: 「自分のレビュー」絞り込みを視点 actor 非依存の reviewer 属性フィルタへ是正・版据え置き)

> 人間が「本システムは認証を持たず『自分』という主体がないので、レビュー待ちは**担当者を指定して絞る**のが要件」と指摘。v19 は §7#18(f)・版ヘッダ・NAMING で「『自分のレビュー』絞り込みは視点 actor(いま誰が見ているか)＝提示層で別途」と記していた。moira-model-update の敵対ループ(R1: adversary×3〔V1–V6〕→ ユーザー裁定 FORK×2 → 著者パッチ → R2: adversary×3〔横断整合/新文面/下流〕→ gate-judge)を経て確定。

- **【ユーザー裁定 FORK①(概念の深さ)】最小訂正を選択**(他候補:認証なしを MODEL に明記)。視点 actor/『自分』という中間概念を外し「per-node `reviewer` 属性を選んで突き合わせる提示層フィルタ」へ。**「認証を持たない」は MODEL に書かない**(射程外)・将来の閲覧者識別拡張も閉じない。
- **【ユーザー裁定 FORK②(確定範囲)】正典矛盾を残さず一括同期を選択**(他候補:MODEL+NAMING のみ先行)。視点 actor 表記が MODEL 自身(版ヘッダ JP/EN・§7 EN gloss)・NAMING に加え surface-schedule Req14・moira-schedule Req14(§7#18(f) を直接引用)・確定済シナリオ・UI-ARCH・UI-DESIGN-BRIEF に散在し、MODEL だけ訂正すると引用先が引用元を否定する自己矛盾になるため。
- **撤回(著者の当初案=過剰主張):** 「本システムは認証を持たない」を MODEL 正典に書く案を撤回——(i) MODEL は認証に一切言及せず意図的スコープ外なのに新前提を密輸、(ii)**認証≠識別**で認証がなくても担当者の選択で絞り込みは成立、(iii) UI-DESIGN-BRIEF「視点 actor は backend に無い」(=未実装)を「概念として恒久的に持たない」へ過剰一般化し将来の閲覧者識別拡張予約を殺す——を R1 で3敵対者が独立に指摘。是正後の §7#18(f) は認証に一切触れない(MODEL の射程外を維持)。
- **撤回(版上げ案):** 当初 v19→v20 と版を上げる案だったが、R2 敵対者が「導出・数式・状態機械・A/I/P/R 番号を一切変えず §7 開示の文面のみ訂正するなら、v16 Phase 0／v16 見積二面性明確化の先例(編集的明確化は版据え置き)に照らし版上げは過剰」と先例照合で指摘。**版据え置き(v19 内訂正)に是正**。版ヘッダ(line 1–8)は『現行版の要約』ゆえ gloss 撤回を反映してよく、v19 来歴行は遡及改変しない。
- **反証された Critical:** (a)「reviewer 名で語ると Actor 型と齟齬・同名衝突」→ 突き合わせを `reviewer` の **Actor {kind,id}** と明記して解消(表示名の一意性に非依存)。(b)「MODEL だけ訂正すると下流が §7#18(f) を引用して自己矛盾」→ 一括同期(FORK②)で解消。(c) 完全性=未指名ノードはどの reviewer 選択にも一致しないが**フィルタで隠さず**『未指名』可視ギャップに残す(P0)を §7#18(f)・各 spec AC に明記。
- **波及同期(本コミット):** MODEL(版ヘッダ JP/EN・§7 intro・§7#18(f)・§7 EN gloss)・NAMING §7 表・surface-schedule Req14(全 AC)・moira-schedule Req14・moira-surface-spec-value Req11 AC4・UI-ARCHITECTURE.md・UI-DESIGN-BRIEF.md・受け入れシナリオ `requirements-spec-drafted`(§1/§4-3/§6 EARS/§7;**§3 不可侵=人間の種は不変**;あわせて R2 で検出した既存の軽微誤り——§6 EARS のトレースと honest-empty 参照 AC——も是正)。
- **別タスク開示:** 参照実装フロントエンド `ScheduleTimeSurface.tsx` のコメント/tooltip(「視点 actor 拡張で解禁」)の reviewer フィルタ化は別タスク(v19 のフロントエンド同期方針を踏襲;『自分』選択肢の disabled 挙動は reviewer 選択フィルタ**未供給**ゆえ現状なお正しく、コメント文言のみ陳腐)。doc ヘッダの版ポインタ(UI-DESIGN-BRIEF「v14」・UI-ARCH/spec「v16」)は本訂正以前からの既存ドリフトで別の doc 衛生タスク。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0(PASS)。**版番号据え置き(v19)**・新公理・新イベント・新状態・新原理番号・新要件番号なし(§7#18(f) の文面訂正のみ)。backend コード変更なし(46 テスト緑のまま)。

## 確定済みの分岐(v17 → v18: 見積カバレッジ P2 を葉基底へ是正)

> 受け入れシナリオ駆動レビュー(kiro-scenario)で人間(外的妥当性オーナー)が「req/design/tasks を全葉合意したのに見積カバレッジが 75%(100% でない)」を指摘し moira-model-update へ ESCALATE。敵対ループ(R1: moira-adversary×3〔V1–V6 分担:最小性·矛盾/完全性·スケール/過剰主張·空虚性〕→ 著者パッチ → 同一ラウンド再反論 moira-adversary×1 → moira-gate-judge PASS)を経て確定。シナリオ層(人間=妥当性)が委譲した正典ギャップを専門家ループが裁定した3例目。

- **【ユーザー裁定 product fork】見積カバレッジ → 「全葉合意=100%」を選択**(他候補:75% 正典維持＋画面明示)。これに伴い P2 を moira-model-update の敵対ゲートにかけ葉基底化を確定。
- **中核の是正:** P2 を**ノード基底**`Σ(合意済み見積ノード)/Σ(既知ツリーの全ノード)` から**葉基底**`|合意済み有効葉|/|既知の有効葉|` へ。参照実装 `coverage.ts` を `effectiveNodes→effectiveLeaves` に同期。根拠=I1 で原始的見積は葉のみ・親は Σ(合意済み子) のロールアップで §7#14(d) のとおり独立合意の対象でない;旧ノード基底は親を分母に数えるため分解木で構造的に 100% 不能(2階層で上限75%)だった(「全葉=100%」と両立には P2 か §7#14(d) の一方変更を要し、P2 側を採った=**設計選択**)。葉基底で EV%(P1)・scheduleCoverage(R-S6)・executionCoverage(R-S8) と有効葉**集合**が統一(測度は EV%=MD 加重・他=カウントで異なり和・交換しない)。§2.3 発見信号は保たれる。
- **Critical「中間ノード合意の不可視=情報損失」を反証→同一ラウンド再反論で SOUND 確認:** 中間ノードへの独立合意は core が構造強制で禁じていない(`fold.ts` の estimate-agreement 分岐は I6=human のみ検査;構造拒否は I2/I6 のみ)が、親見積は I1 で「Σ(合意済み子)」と**規範上**定まるため親合意は冗長 or stale で、葉基底が無視するのは全ケースで正しい coverage(stale 合意親/中間直接合意/子全合意 を検証)。**情報損失でなく §7#14(d) との整合回復**と決着。新不変条件(中間合意の構造禁止)は**最小性により足さず** §7#17(b) に deferred 開示。
- **再反論で検出・解消した Important:** (i) 反証の過剰表現「親値は I1 で**完全決定**(実装でも保証)」を是正——`fold.ts` は親見積を Σ 再計算しないため stale 親 estimateState/frozenBudget が `computeNodeStates`(node-state 読み)に残る。P2 coverage でも他の葉基底導出でもなく **I1 実装 enforcement/提示**の既存事項(正常系=feature 先 decompose では親 never-agreed で非発生;発生は agree→decompose 等の異常系)で **P2 スコープ外として deferred**。(ii) fixture `tiny-project.ts` L21 の旧 5/6 コメント残存→ 1.0 へ修正。(iii)「旧ノード基底は §7#14(d) と内的に矛盾」を「両立に一方変更を要する**設計選択**」へ緩和(P2本文/§6/§7#17、JA+EN)。
- **撤回(自己の過去正典):** P2 ノード基底定義、`coverage.ts` コメント「independently-agreed effective nodes / known effective nodes」、受け入れシナリオ単位(estimate-spec-agreed §7・requirements-spec-drafted)の「P2 はノード基底=75% が canon の設計」。
- **波及同期:** MODEL P2/§2.3/§6/§7#17/版ヘッダ・NAMING §7 表・`coverage.ts`・`coverage.test.ts`/`golden.test.ts`・`tiny-project.ts`・thought-experiment-11 を本コミットで同期(全 46 backend テスト緑)。受け入れシナリオ 3 単位(estimate-proposed/agreed・requirements-spec-drafted)の 75%→100% 同期は **kiro-scenario 所管の別タスク**。
- **残余開示(§7#17):** 葉基底で前倒し合意も 100% に達しうる(成熟度を P2 は区別しない=§7#15(a) 同型の無警告楽観)/中間合意の stale 親 estimateState の扱い(presentation / fold 無効化 / 警告)は I1 enforcement の deferred 事項。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0(PASS;参照実装 coverage.ts/effective-set.ts/fold.ts/ev.ts と照合)。新公理・新イベント・新状態・新原理番号・新要件番号なし(P2 基底の是正のみ)。

## 確定済みの分岐(v19 editorial: クリティカルパスへのエージェント算入を de-condition)

> PROPERTIES.md の未 bound 穴埋めレビュー中、人間(プロダクトオーナー)が PR-CRITPATH-AGENT の「人間後続を律速する場合(A→H)に限り算入」という留保に疑問を呈し、「クリティカルパス可視化はバッファ管理の前提として実現すべき・エージェント所要時間は実時間ゆえ常に算入されるべき」と判断。moira-model-update の敵対ゲート(R1: moira-adversary×3〔V1–V6 分担:最小性/空虚性・矛盾/完全性・過剰主張/同期網羅〕→ 著者パッチ → moira-gate-judge PASS)を経て確定。

- **【ユーザー裁定 FORK①】版の扱い → 「editorial・v19 据え置き」を選択**(他候補:semantic として v20 へ版上げ)。根拠=P7 第2文が既に「全依存連鎖の最長路」と無条件を明言し、参照実装 `leveler.ts` の `cp=durationDays+max(全後続)`(agent `durationDays=ceil(est)`)も既に無条件——内的矛盾(条件付き例 vs 無条件定義)を定義側へ解消する editorial であり導出・数式・状態機械を変更しない(v16 Phase 0 同型)。
- **【著者裁定 FORK②】tail エージェント=deliverable か housekeeping か → 区別を持たない**。A1(全ノードは遂行され出来高を生む作業)＋最小性により、全有効・スケジュール済み葉が D_pred に寄与する(新軸を足さない=空虚な P0 溶解でない)。
- **中核の是正:** P7 本文・R-T2(JP/EN) を「後続の種別を問わず——後続が無くても——パス長へ寄与・A→H は代表的構成であって寄与の条件でない」へ。過剰主張を封じ込め『**置換辺=supersede は含まない**(R-D7/§2.7)』『寄与の対象は**有効・割当済みの葉に限る**(P0;未割当・cancelled は §3/R-C2)』を明記し「無条件」の射程を後続種別のみに限定。P7 タイトルを「人間接点が**主たる**律速」へ、R-T4 括弧注を「末端・中間いずれにあっても」へ同期。実装注意・バグ来歴は P7 定義本文へ混ぜず §6 来歴へ集約(敵対者の形式違反指摘を是正)。
- **動機(falsifiable):** 条件付き(A→H 限定)の字義実装は末尾エージェント(人間後続なし)を D_pred から漏らし、R-T4 期日超過アラートの偽陰性(v10→v11／思考実験07 が是正したバグ)を再発させる。本 editorial はその予防＝仕様の明確化。
- **波及同期:** `leveler.ts` の条件付きコメント(2箇所)を是正、`PROPERTIES.md` PR-CRITPATH-AGENT を同一 run で再批准(無条件・supersede 除外・スケジュール領域限定を反映、`proposed` 維持・★impl-pending 解除)。
- **別タスク(開示):** 下流 spec `moira-schedule/requirements.md`(Req2 AC3・Req8 AC1 の "rate-limiting a human successor" 条件付き文言)の同期と、NAMING §7 への「クリティカルパス／D_pred」語彙追加は kiro 系所管の別タスク。
- **impl 状態の正直化:** D_pred の集約 read・R-T4 アラートは現状未実装(§7#10・PROPERTIES ★impl-pending)。クリティカルパス自体は leveler が cp／予測完了を算出済み。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0(PASS)。**版番号据え置き(v19)**・新公理・新イベント・新状態・新原理番号・新要件番号なし(P7/R-T2 の意味論明確化のみ)。

## 確定済みの分岐(v19 → v20: 所属の latest-wins 置換)

> 【転記注記】本節は issue #19 追いつき(2026-07-05)で、MODEL §6 の v19→v20 来歴(bb81ff2・2026-07-03 確定)から**純転記**したもの(新規主張なし)。v20 確定時にジャーナルへの追記が漏れていた同期漏れの解消。詳細・一次記録は MODEL §6 当該行を正とする。

- **起点:** playground 実運用(cc-sdd×Moira 体験フロー)で `moira add`(--parent 省略)が root への第二の所属辺を張り、木の二重表示・acByNode の二重計上・「訂正イベントが無く会計中立には回復できない」補償被覆漏れが露出(GitHub issue #5。cancelled+再作成という追記のみの回復経路は当時も存在したが会計中立でない)。
- **当初案の撤回:** 「DecomposeEvent に `op=detach`(補償的取消)を導入」は独立敵対ゲート R1(moira-adversary×3・V1–V6)で反証され**全面撤回**——(1) 必要性未立証(latest-wins 単一有効親の下では正しい親への再 decompose 1発で補償が閉じ、detach 固有能力は駆動 UC の無い「孤児化」のみ)、(2) detach の旧辺復活則が有効親循環を作る具体反例(2循環島の構成)、(3) 多重親状態の I1 Σ/P3 AC 二重計上が未定義。
- **中核の確定:** 対抗案「**所属の latest-wins 置換**」へ差し替え。R2(moira-adversary×2)の Critical——所属循環が無防備(単一有効親は木を保証しない;I2 は relate 限定)・帰結リスト不完全(新親の脱葉化・完了葉の誤内部化の EV_abs 消失)・見積・所属の同一イベント連動未規律——をパッチ: §2.8 に**木性の保護**(循環 decompose の拒否)・per-child 意味論(「名指し漏れによる除去」の表現不能化=取消イベントの裏口封じ)・イベント内末尾勝ち・帰結の両側開示;**cost 額の非負(A6)**;§5 の検証不能な来歴的主張の除去。
- **暫定裁定:** genuine 分岐4件(所属競合の警告有無/意図的 re-parent の地位/cost 符号/見積連動規律)は AskUserQuestion で2度ユーザーに提示・無応答のため、推奨最小案(沈黙 latest-wins+開示/正当な構造編集/非負正典化/開示のみ)を**暫定裁定**として採用し §7#19 に再訪可を明記。
- **遡及解釈変更:** pre-v20 ログの異主語 decompose 群は「最後の親への単一所属」として読み直される(fold の導出規則変更。イベント自体は不改変=A2 保持;§7#19(g))。
- **波及同期(v20 コミット):** MODEL(ヘッダ/§2.6/§2.8/§5/§7#17(b)/§7#19)・参照実装 `fold.ts`(latest-wins 置換・木性ガード・負 cost 拒否)＋テスト(fold.test 7本・PM-TREE-INV・PR-REPARENT-HEAL・CLI golden 補償シナリオ)・PROPERTIES.md(新規2行＋被覆)・DECISIONS-CATALOG(D-63)。
- **別タスク開示(v20 時点):** agreed シナリオ単位 `manager-spots-review-bottleneck` の e210 が非準拠の decompose 形を「実在イベント」と主張している件の kiro-scenario 同期。`moira-track` テンプレの §F 復旧手順は v20 コミットで再 add 方式へ更新済み・doc-refine 再凍結は後続。

独立採点者(moira-gate-judge)の残存 Critical/Important = 0(PASS)。**版上げ(v19→v20)**。新イベント・新状態・新要件番号なし(§2.8 所属意味論の確定と A6 への非負の明記)。

## 未解決の論点 / 次の一手

- **次の一手**:検証シナリオ **S4(健全性のリアルタイム把握)を一本貫き**、4イベントを fold して EV%+カバレッジ+SPI/CPI を導出する最小バックエンドで「イベント→導出→表示」の型を作る。
- **次の一手(v18 シナリオ単位同期)**:P2 葉基底化に伴い、受け入れシナリオ単位 `estimate-spec-proposed.md`・`estimate-spec-agreed.md`・`requirements-spec-drafted.md` の「P2 ノード基底=75%」表記を 100%(葉基底)へ同期する(kiro-scenario 所管)。本タスクは MODEL と派生確定文書のみ確定。
- **次の一手(v17 下流同期)**:v17 で R-T5 が「作業開始遷移 **or** 状態保持遷移(暫定割当=to=現状態)」へ、見積が「入力連鎖=理想形・前倒し許容(§2.3/R-E3 incomplete 限定)」へ拡張されたため、下流 spec の文言を同期する——`moira-schedule/requirements.md`(assignee を "lifecycle transition into the active state" とのみ規定)・`moira-core` R-T5 系・`moira-ingestion-adapter`(§7#14(f) 既出)。本タスクは MODEL と派生文書のみ確定し、下流 spec 同期は別タスクとして切り出し済み(gate-judge も対象外と確認)。
- **保留:Moira と既存 EVM Studio 系 spec の関係。** 既存 `evm-studio` / `sdd-dashboard` は使い捨てプロトタイプ実験(スナップショット方式・単一アクター)であり、Moira はその失敗を踏まえた**完全新規・別概念**。具体アーキテクチャは MODEL を前提に今後検討。
- **運用上の確認事項**(MODEL 構造には影響しない):
  - スロット凍結の「意味のある初回スケジュール」(暫定割当 vs 着手時)は P0=プロジェクト方針として選ぶ(MODEL §3 に委譲済み)。
  - 辺ポリシー既定の上書き対象(楽観が成り立たない特別な依存)はプロジェクトごとに明示。

## メモ
- MODEL の改訂は必ず `moira-model-update` スキルを通す(独立敵対者ゲート、Opus 4.8+/effort max 推奨)。
- 命名・本スキル自身・エージェント定義も同スキルの対象(対象種別で確定手順が分岐)。
